function extract(startingYieldOnFirstDay) {
    
    const totalAmount = 0;
    const yield = startingYieldOnFirstDay;
    const consumptionOfMiningCrew = 26;
    const exhaustionConsumption = 26;

    while (yield >= 100) {
        totalAmount
        yield - 
    }
}

extract(111);   // 2    134 
extract(450);   // 36   8938





// Day 1 we extract 111 spices and at the end 
// of the shift, the workers consume 26, leaving 85. 
// The yield drops by 10 to 101. 
// On day 2 we extract 101 spices, the workers consume 26, 
// leaving 75. The total is 160 and the yield has dropped to 91.
// Since the expected yield is less than 100, we abandon the source. 
// The workers take another 26, leaving 134. The mine 
// has operated for 2 days.
